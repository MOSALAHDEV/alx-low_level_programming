#include "main.h"
/**
 *_abs - compute the absolute value of intger
 *@n: is the number turned to abs value
 *
 *Return: the absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
